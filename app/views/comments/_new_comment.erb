<%= form_for Comment.new, remote: :true do |f| %>
    <%= f.hidden_field :improvement_id, value: @improvement.id %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.label :content, "Conteúdo" %><br />
    <%= f.text_area :content %>

    <%= f.submit "Novo Comentário", class: "btn btn-primary" %>
<% end %>

<script>
    $('form').bind('ajax:complete', function(xhr, result){
    $('#comments').replaceWith(result.responseText);
    $('textarea').val("");
    });
</script>